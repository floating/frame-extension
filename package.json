{
  "name": "frame-extension",
  "version": "0.0.2",
  "description": "",
  "main": "src",
  "scripts": {
    "test": "standard",
    "build": "rimraf ./dist && parcel build ./src/frame.js --no-source-maps --target browser && node ./src/inline.js"
  },
  "author": "Jordan Muir",
  "license": "GPL-3.0",
  "devDependencies": {
    "ncp": "^2.0.0",
    "parcel-bundler": "^1.8.1",
    "rimraf": "^2.6.2",
    "standard": "^11.0.1"
  },
  "dependencies": {
    "eth-provider": "0.0.7",
    "web3": "^0.20.7"
  }
}
